spring:
  datasource:
    url: jdbc:mysql://localhost:3306/team_E
    username: ENC(${DB_USER})
    password: ENC(${DB_PW})
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace



